module.exports = {
    name: 'check',
    description: 'Sprawdź, czy ktoś z lobby znajduje się w liście zjebów.',
    args: true,
    execute(message, args) {
        // if (args[0] === 'foo') {
        //     return message.channel.send('bar');
        // }
        // message.channel.send(`Pierwszy argument: ${args[0]}`);
    },
};